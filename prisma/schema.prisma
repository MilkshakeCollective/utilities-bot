generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model GuildSettings {
  guildId          String  @id
  roadmapMessageId String?
}

model RoadmapItem {
  id        String   @id
  title     String
  link      String?
  status    String   @default("🍼 Not started")
  createdAt DateTime @default(now())
}
